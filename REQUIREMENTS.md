# カロリー記録チャットアプリ 要件定義

## 概要

AIチャットで食事・運動を記録できるアプリ(Journable等)の類似アプリ。個人利用のみ・完全無料での運用を前提とし、外部AI APIは使わずに自作DB+キーワードマッチングでカロリーを算出する。

## 技術構成

- 静的HTML/CSS/JS(`index.html` / `style.css` / `app.js` / `data.js`)のみ。ビルド不要、`index.html`をブラウザで直接開くだけで動作する(サーバー不要)。
- データはブラウザの`localStorage`に保存(キー: `calorieChatData`)。
- 外部AI API・サーバーは使用しない。

## UI要件

- 全体はJournableのスクリーンショットを参考にした、スマホアプリ風のカードフィードUI(中央寄せ、丸みのあるカード、影付き)。チャット吹き出し形式ではない。
- **ヘッダー**: ☰メニュー(プロフィール編集)、日付タイトル(タップで今日に戻る)
- **週の日付ストリップ**: 月〜日の7セル、‹›で週送り。**その日に記録がある日だけ薄緑**、今日は常に濃い緑(週末だからではない)
- **カロリーサマリーカード**: 🔥カロリー、進捗バー(緑)、「食事/運動/残り」の3項目、その下に**PFCバランス**(たんぱく質/脂質/炭水化物ごとに色分けした実績/理想のミニバー3本。理想は目標カロリーをP30%・F20%・C50%(3:2:5)で配分してグラム数に逆算したもの。理想超過時はバーが100%で頭打ちになるが数値側で確認できる)
- **ログフィード**: 1メッセージ=1カード。カード内に元の入力文(薄いグレー)→品目名+数量(太字)→カロリー/炭水化物/タンパク質/脂質のチップ、という構成。運動は消費カロリー+時間(または重量/回数/セット)のチップ
- 各ログ項目に **✎編集** / **✕削除** ボタン。編集は品目名・数量表示・カロリー・PFCを直接書き換えて保存可能
- 認識できなかった品目には **「+ 手動で追加」** ボタンが出る(手動入力フォームは維持)。隣に**✕(手動入力ボタンだけを消す)**ボタンがあり、手動入力するつもりがない場合はこれで個別に非表示にできる(ログ自体の削除とは別操作)
- **入力欄**: テキスト入力+送信ボタン(丸型)。
  - **フォーカス時、入力が空なら**直近の食事/運動履歴が各10件チップで表示され、クリックで入力欄に反映(送信はされない)
  - **入力中は**、入力末尾に前方一致する食品/運動DBの候補が最大6件チップで表示される(履歴表示と同じ欄を動的に切り替え)。クリックすると該当部分がその品目名に置き換わる(送信はされない、続けて数量や次の品目を入力できる)
- 認識結果の表示: 全く認識できなければ「認識できませんでした」、一部だけ認識できなければ「一部、認識できなかった内容があります」と表示
- **初回起動時のみ**プロフィール入力画面(性別・年齢・身長・体重・目標体重・活動レベル)が出る。以降は☰メニュー→「プロフィールを編集」からいつでも変更可能(毎回は聞かれない)
- 過去日を選択すると入力欄は非表示(閲覧専用)

## カロリー計算ロジック

- **目標カロリー**: Mifflin-St Jeor式でBMRを算出 → 活動レベル係数(5段階: 座りがち1.2/軽い運動1.375/ふつう1.55/活発1.725/非常に活発1.9)でTDEEを推定 → 目標体重との差分 ×7,700kcal/kg を365日で日割りしてTDEEに加算(下限1,200kcal)
- **食品**: DBに一致した品目のkcal/PFCを、指定された数量(個数、g指定、分数「1/2人前」、半分/半人前、小数「0.5人前」)に応じて計算
- **運動(通常)**: MET×体重×時間で算出。時間指定がなければ品目ごとのデフォルト時間を使用
- **運動(筋トレ・ジムマシン)**: 重量・回数・セット数が指定されていれば、「セット×(回数×3秒+セット間休憩90秒)」を運動時間とみなし、体重に対する挙上重量の比率で強度係数を掛けて算出。指定がなければ通常運動と同じMET×時間の計算にフォールバック

## 入力解析の仕様

- 1メッセージに複数品目(食事・運動どちらも)を含められる(例:「白米と味噌汁、ウォーキング30分」)
- メッセージの左から右へ処理し、数量トークン(個数・g・分/セット等)が別の品目に誤って使われないよう、次の品目の開始位置より先は探索しない
- **あいまい検索**: カタカナ⇔ひらがな、全角⇔半角、長音記号(ー)の母音展開(「とーふ」→「とうふ」)を正規化で自動吸収。さらに完全一致がなければ編集距離ベースでタイプミスも拾う(4文字以上の単語のみ対象。3文字以下は誤爆リスクが高いため対象外)
- セット数は「3セット」「×3」「x3」いずれの表記にも対応
- `unit`フィールドには**数字を含めてはいけない**(アプリ側が`${数量}${unit}`の形で結合するため。過去に複数回バグの原因になった)

## DBの内容(2026年7月時点)

- **食品 354品目**: 一般料理(和食・麺類・丼・主菜副菜・ファストフード・スイーツ・飲料等)/ チェーン店14社超(吉野家・すき家・松屋・マクドナルド・モスバーガー・KFC・サブウェイ・スターバックス・ドトール・セブン/ローソン/ファミマ・CoCo壱番屋・丸亀製麺・すかいらーく系・サイゼリヤ・和幸・ゴンチャ・魚べい)/ 文部科学省食品成分DBベースの生鮮食材40品目 / 市販品(プロテイン・カップ麺インスタント・お菓子スナック・アイス冷凍食品、期間限定除く)/ まいばすけっと弁当惣菜30品目
- **運動 92種目**: 一般運動・ジムマシン&フリーウェイト(重量計算対応)・一般スポーツ・セルフエステ(部位別)
- 主要ブランドは「ブランド名単体」でも代表商品に一致するようエイリアス調整済み(例:「吉野家」→牛丼並盛)

## 既知の制限・データ品質の注意

- 一部チェーン(ガスト・サイゼリヤ・和幸・ゴンチャ等)や まいばすけっとの一部品目は、公式にカロリーまたはPFCが非公開のため**推定値**を使用している
- 推定値かどうかのマーキングは**セクション単位/品目単位で不統一**(まいばすけっとの品目のみ`// ※推定`コメントあり、他チェーンはセクション冒頭コメントのみ)。今後統一する余地あり
- エイリアス重複・誤爆チェックの自動化ツールは未整備(追加のたびに手動でブラウザ確認している)

## 意図的に実装していない機能

Journableにはあるが、個人利用・無料という要件に合わないため見送っているもの:

- 写真解析によるカロリー記録
- 友達・ストリーク・共有ボタン・プレミアム誘導・水分トラッカー・自動生成レポート
